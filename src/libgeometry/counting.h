#ifndef COUNTING_H
#define COUNTING_H

#include <string>
#include <vector>

const float pi = 3.1415;

float perim(std::vector<float>& coords);
float area(std::vector<float>& coords);

#endif // !COUNTING_H
